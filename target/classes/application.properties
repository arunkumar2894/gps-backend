# Port (optional)
server.port=8080

# Allow both /secrets and /secrets/ to match
spring.mvc.trailing-slash-match=true
api.url=http://localhost:8080

# Token required to update secrets
app.update.token=secret-token-123
